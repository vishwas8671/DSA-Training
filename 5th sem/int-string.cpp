#include <iostream>
using namespace std;

int main() {
    int n, num, str;
    char type;
    int arr1[1000];
    char arr2[1000];
    cout << "Enter the size of the array : ";
    cin >> n;
    cout << "Enter the type of the array : ";
    cin >> type;
    if (type = num) {
        for (int i=0; i<n; i++) {
            cout << arr[i];
        }
    }
}
