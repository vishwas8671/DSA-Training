#include <bits/stdc++.h>
using namespace std;

int main () {
    int a[100];
    int n, a;
    cout << "Number of elemts are : " << n;
    for (int i=0; i<n; i++) {
        cin >> a[i];
    }

    if (a[i] < 2) {
        cout << "Array size is not sufficuent : " << endl;
    }
    else if (a[i]==2) {

    }
     else {
        int sum_first = a[0] + a[1];
        int last_sum = a[n] + a[n-1];
        cout << "sum of first two elemnts : " << 


     }
    


}
